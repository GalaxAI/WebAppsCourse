<div class="container">
    <h1 class="page-title"><%= title %></h1>
    <p class="lead-text"><%= message %></p>
    
    <div class="card">
        <h2 class="card-header">Lista przykładowa:</h2>
        <ul class="item-list">
            <% items.forEach(function(item) { %>
                <li class="item"><%= item %></li>
            <% }); %>
        </ul>
    </div>
</div>